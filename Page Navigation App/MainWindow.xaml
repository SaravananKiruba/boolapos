<!--/// <Summary>
    /// Author : R. Arun Mutharasu
    /// Created :25-08-2022
    /// YouTube Channel : C# Design Pro 
    /// </Summary>-->

<Window x:Class="Page_Navigation_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Page_Navigation_App.ViewModel"
        xmlns:Menu="clr-namespace:Page_Navigation_App.Utilities"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="600"
        Width="850"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent">

    

    <Grid>

        <!--// Window Border //-->

        <Border Background="#212529"
                CornerRadius="20" />

        <Grid>

            <!--// Base Grid //-->

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="228" />
                <ColumnDefinition Width="622" />
            </Grid.ColumnDefinitions>

            <!--// Navigation Panel //-->

            <Grid HorizontalAlignment="Left" Width="228" Background="#1E1E2F">
                <Border Background="#2A2A3D" CornerRadius="20,0,0,20">
                    <StackPanel>
                        <!-- Redesigned Sidebar with Modern UI -->
                        <Button Style="{StaticResource BtnStyle}" Command="{Binding HomeCommand}" Margin="10,10,10,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="Images/img_home.png" Width="24" Height="24" Margin="0,0,10,0"/>
                                <TextBlock Text="Home" Foreground="#FFFFFF" FontSize="16" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource BtnStyle}" Command="{Binding CustomersCommand}" Margin="10,10,10,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="Images/img_customer.png" Width="24" Height="24" Margin="0,0,10,0"/>
                                <TextBlock Text="Customers" Foreground="#FFFFFF" FontSize="16" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource BtnStyle}" Command="{Binding ProductsCommand}" Margin="10,10,10,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="Images/img_product.png" Width="24" Height="24" Margin="0,0,10,0"/>
                                <TextBlock Text="Products" Foreground="#FFFFFF" FontSize="16" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource BtnStyle}" Command="{Binding OrdersCommand}" Margin="10,10,10,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="Images/img_order.png" Width="24" Height="24" Margin="0,0,10,0"/>
                                <TextBlock Text="Orders" Foreground="#FFFFFF" FontSize="16" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource BtnStyle}" Command="{Binding TransactionsCommand}" Margin="10,10,10,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="Images/img_transaction.png" Width="24" Height="24" Margin="0,0,10,0"/>
                                <TextBlock Text="Transactions" Foreground="#FFFFFF" FontSize="16" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Column="1">

                <ContentControl x:Name="Pages"
                                Content="{Binding CurrentView}" />

            </Grid>

        </Grid>

        <Button x:Name="CloseApp"
                Style="{StaticResource ExitApp}"
                Click="CloseApp_Click" />

    </Grid>

</Window>
