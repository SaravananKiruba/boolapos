<UserControl x:Class="Page_Navigation_App.View.Vendors"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <Grid Margin="15" Background="#1E1E2F">
        <Border CornerRadius="20" BorderBrush="#4A4A6A" BorderThickness="2" Padding="20" Background="#2A2A3D">
            <StackPanel>
                <TextBlock Text="Vendor Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="#FFFFFF"/>

                <!-- Vendor Details Section -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Vendor Name:" Width="150" VerticalAlignment="Center" Foreground="#FFFFFF"/>
                    <TextBox Width="300" Height="30" Text="{Binding SelectedVendor.VendorName, UpdateSourceTrigger=PropertyChanged}" Background="#3A3A4D" Foreground="#FFFFFF" BorderBrush="#4A4A6A"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Phone Number:" Width="150" VerticalAlignment="Center" Foreground="#FFFFFF"/>
                    <TextBox Width="300" Height="30" Text="{Binding SelectedVendor.PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Background="#3A3A4D" Foreground="#FFFFFF" BorderBrush="#4A4A6A"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Contact Person:" Width="150" VerticalAlignment="Center" Foreground="#FFFFFF"/>
                    <TextBox Width="300" Height="30" Text="{Binding SelectedVendor.ContactPerson, UpdateSourceTrigger=PropertyChanged}" Background="#3A3A4D" Foreground="#FFFFFF" BorderBrush="#4A4A6A"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Email:" Width="150" VerticalAlignment="Center" Foreground="#FFFFFF"/>
                    <TextBox Width="300" Height="30" Text="{Binding SelectedVendor.Email, UpdateSourceTrigger=PropertyChanged}" Background="#3A3A4D" Foreground="#FFFFFF" BorderBrush="#4A4A6A"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Address:" Width="150" VerticalAlignment="Top" Foreground="#FFFFFF"/>
                    <TextBox Width="300" Height="60" Text="{Binding SelectedVendor.Address, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Background="#3A3A4D" Foreground="#FFFFFF" BorderBrush="#4A4A6A"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Add/Update" Width="150" Height="35" Background="#28A745" Foreground="#FFFFFF" FontWeight="Bold" BorderBrush="#218838" Command="{Binding AddOrUpdateCommand}"/>
                    <Button Content="Clear" Width="150" Height="35" Background="#DC3545" Foreground="#FFFFFF" FontWeight="Bold" BorderBrush="#C82333" Command="{Binding ClearCommand}" Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Vendor List Section -->
                <ListView ItemsSource="{Binding Vendors}" Margin="0,20,0,0" Background="#3A3A4D" Foreground="#FFFFFF" BorderBrush="#4A4A6A">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding VendorName}" Width="150"/>
                            <GridViewColumn Header="Phone" DisplayMemberBinding="{Binding PhoneNumber}" Width="100"/>
                            <GridViewColumn Header="Contact" DisplayMemberBinding="{Binding ContactPerson}" Width="150"/>
                            <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="150"/>
                            <GridViewColumn Header="Address" DisplayMemberBinding="{Binding Address}" Width="200"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>