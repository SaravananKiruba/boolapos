<UserControl x:Class="Page_Navigation_App.View.Products"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Page_Navigation_App.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:ProductVM />
    </UserControl.DataContext>

    <Grid Margin="15" Background="#212529">
        <!-- Main Border for Product Form -->
        <Border CornerRadius="20" BorderBrush="#6C757D" BorderThickness="2" Padding="20" Background="#343A40">
            <StackPanel>
                <TextBlock Text="Product Information Form" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="#F8F9FA"/>

                <!-- Product Name -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Product Name:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <TextBox Width="300" Height="30" Name="ProductNameTextBox" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D"/>
                </StackPanel>

                <!-- Unique Tag ID -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Unique Tag ID:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <TextBox Width="300" Height="30" Name="TagIDTextBox" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D"/>
                </StackPanel>

                <!-- Product Picture -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Product Picture:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <Button Content="Upload Picture" Width="150" Height="30" Background="#6C757D" Foreground="#F8F9FA" FontWeight="Bold" BorderBrush="#495057"/>
                </StackPanel>

                <!-- Category -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Category:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <ComboBox Width="300" Height="30" Name="CategoryComboBox" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D">
                        <ComboBoxItem Content="Electronics"/>
                        <ComboBoxItem Content="Clothing"/>
                        <ComboBoxItem Content="Furniture"/>
                    </ComboBox>
                </StackPanel>

                <!-- Sub Category -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Sub Category:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <ComboBox Width="300" Height="30" Name="SubCategoryComboBox" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D">
                        <ComboBoxItem Content="Mobile Phones"/>
                        <ComboBoxItem Content="Laptops"/>
                        <ComboBoxItem Content="Accessories"/>
                    </ComboBox>
                </StackPanel>

                <!-- Add Button -->
                <Button Content="Add Product" Width="150" Height="35" Margin="0,20,0,0" HorizontalAlignment="Center" Background="#198754" Foreground="#F8F9FA" FontWeight="Bold" BorderBrush="#157347"/>
            </StackPanel>
        </Border>
    </Grid>

</UserControl>
