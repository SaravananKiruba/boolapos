<UserControl x:Class="Page_Navigation_App.View.Products"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <Grid Margin="15" Background="#212529">
        <!-- Main Border for Product Form -->
        <Border CornerRadius="20" BorderBrush="#6C757D" BorderThickness="2" Padding="20" Background="#343A40">
            <StackPanel>
                <TextBlock Text="Product Management" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="#F8F9FA"/>

                <!-- Product Details Section -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Product Name:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <TextBox Width="300" Height="30" Text="{Binding SelectedProduct.ProductName, UpdateSourceTrigger=PropertyChanged}" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Price:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <TextBox Width="300" Height="30" Text="{Binding SelectedProduct.Price, UpdateSourceTrigger=PropertyChanged}" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Category:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <ComboBox Width="300" Height="30" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedProduct.Category}" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Stock Level:" Width="150" VerticalAlignment="Center" Foreground="#F8F9FA"/>
                    <TextBox Width="300" Height="30" Text="{Binding SelectedProduct.StockLevel, UpdateSourceTrigger=PropertyChanged}" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Add/Update" Width="150" Height="35" Background="#198754" Foreground="#F8F9FA" FontWeight="Bold" BorderBrush="#157347" Command="{Binding AddOrUpdateCommand}"/>
                    <Button Content="Clear" Width="150" Height="35" Background="#DC3545" Foreground="#F8F9FA" FontWeight="Bold" BorderBrush="#C82333" Command="{Binding ClearCommand}" Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Product List Section -->
                <ListView ItemsSource="{Binding Products}" Margin="0,20,0,0" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding ProductName}" Width="150"/>
                            <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price}" Width="100"/>
                            <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category.Name}" Width="150"/>
                            <GridViewColumn Header="Stock" DisplayMemberBinding="{Binding StockLevel}" Width="100"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>

</UserControl>
