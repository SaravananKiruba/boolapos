<UserControl x:Class="Page_Navigation_App.View.Orders"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Page_Navigation_App.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:OrderVM />
    </UserControl.DataContext>

    <Grid Margin="15" Background="#212529">
        <Border CornerRadius="20" BorderBrush="#6C757D" BorderThickness="2" Padding="20" Background="#343A40">
            <StackPanel>
                <TextBlock Text="Order Management" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="#F8F9FA"/>

                <!-- Search Order Section -->
                <StackPanel Orientation="Horizontal" Margin="0,5" HorizontalAlignment="Center">
                    <Label Content="Search Order ID:" Foreground="#F8F9FA" VerticalAlignment="Center"/>
                    <TextBox Width="200" Height="30" Text="{Binding SearchOrderID}" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D" Margin="5,0"/>
                    <Button Content="Search" Width="100" Height="30" Background="#198754" Foreground="#F8F9FA" BorderBrush="#157347" Margin="5,0" Command="{Binding SearchCommand}"/>
                </StackPanel>

                <!-- Order List Section -->
                <ListView ItemsSource="{Binding Orders}" Margin="0,20,0,0" Background="#495057" Foreground="#F8F9FA" BorderBrush="#6C757D">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Order ID" DisplayMemberBinding="{Binding OrderID}" Width="100"/>
                            <GridViewColumn Header="Customer" DisplayMemberBinding="{Binding Customer.CustomerName}" Width="150"/>
                            <GridViewColumn Header="Order Date" DisplayMemberBinding="{Binding OrderDate}" Width="150"/>
                            <GridViewColumn Header="Total Amount" DisplayMemberBinding="{Binding TotalAmount}" Width="100"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Add Order" Width="150" Height="35" Background="#0D6EFD" Foreground="#F8F9FA" FontWeight="Bold" BorderBrush="#0A58CA" Command="{Binding AddOrderCommand}"/>
                    <Button Content="Delete Order" Width="150" Height="35" Background="#DC3545" Foreground="#F8F9FA" FontWeight="Bold" BorderBrush="#C82333" Command="{Binding DeleteOrderCommand}" Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
