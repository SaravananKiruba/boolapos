<?xml version="1.0" encoding="UTF-8"?>
<Application x:Class="Page_Navigation_App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Page_Navigation_App"
             xmlns:view="clr-namespace:Page_Navigation_App.View"
             xmlns:vm="clr-namespace:Page_Navigation_App.ViewModel"
             xmlns:utilities="clr-namespace:Page_Navigation_App.Utilities"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Application.Resources>
        <ResourceDictionary>
            <!-- MahApps.Metro ResourceDictionaries -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.Buttons.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.DataGrid.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Blue.xaml"/>
                <!-- Local Theme Resources -->
                <ResourceDictionary Source="/Styles/Controls.Themes.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- View-ViewModel Mappings -->
            <DataTemplate DataType="{x:Type vm:HomeVM}">
                <view:Home/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:CustomerVM}">
                <view:Customers/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ProductVM}">
                <view:Products/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:OrderVM}">
                <view:Orders/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TransactionVM}">
                <view:Transactions/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SupplierVM}">
                <view:Suppliers/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:RateMasterVM}">
                <view:RateMaster/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:RepairJobVM}">
                <view:RepairJobs/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:StockVM}">
                <view:Stock/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:CategoryVM}">
                <view:Categories/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:UserVM}">
                <view:Users/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReportVM}">
                <view:Reports/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsVM}">
                <view:Settings/>
            </DataTemplate>

            <!-- Converters -->
            <utilities:ViewModelToTitleConverter x:Key="ViewModelToTitleConverter"/>

            <!-- Theme Colors -->
            <Color x:Key="Primary">#FF2D98DA</Color>
            <Color x:Key="PrimaryLight">#FF74B9FF</Color>
            <Color x:Key="PrimaryDark">#FF2475A2</Color>

            <!-- Background & Surface -->
            <Color x:Key="Background">#FF45AAF2</Color>
            <Color x:Key="Surface">#FFFFFFFF</Color>
            <Color x:Key="SurfaceSecondary">#FFF1F2F6</Color>

            <!-- Text -->
            <Color x:Key="TextPrimary">#FFFFFFFF</Color>
            <Color x:Key="TextSecondary">#FFDFF1FF</Color>
            <Color x:Key="TextOnSurface">#FF2D3436</Color>

            <!-- Status Colors -->
            <Color x:Key="Success">#FF26DE81</Color>
            <Color x:Key="Warning">#FFF1C40F</Color>
            <Color x:Key="Error">#FFEB3B5A</Color>
            <Color x:Key="Info">#FF00CEC9</Color>

            <!-- Base Brushes -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary}"/>
            <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLight}"/>
            <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDark}"/>
            <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource Background}"/>
            <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource Surface}"/>
            <SolidColorBrush x:Key="SurfaceSecondaryBrush" Color="{StaticResource SurfaceSecondary}"/>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
            <SolidColorBrush x:Key="TextOnSurfaceBrush" Color="{StaticResource TextOnSurface}"/>
            <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource Success}"/>
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource Warning}"/>
            <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource Error}"/>
            <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource Info}"/>

            <!-- MahApps.Metro Theme Override Colors -->
            <Color x:Key="MahApps.Colors.ThemeBackground">#45aaf2</Color>
            <Color x:Key="MahApps.Colors.AccentBase">#2d98da</Color>
            <Color x:Key="MahApps.Colors.Accent">#2d98da</Color>
            <Color x:Key="MahApps.Colors.Accent2">#74b9ff</Color>
            <Color x:Key="MahApps.Colors.Accent3">#2475a2</Color>
            <Color x:Key="MahApps.Colors.Highlight">#74b9ff</Color>

            <!-- MahApps.Metro Theme Override Brushes -->
            <SolidColorBrush x:Key="MahApps.Brushes.ThemeBackground" Color="{StaticResource Background}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Window.Background" Color="{StaticResource Background}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.AccentBase" Color="{StaticResource Primary}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Accent" Color="{StaticResource Primary}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Accent2" Color="{StaticResource PrimaryLight}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Accent3" Color="{StaticResource PrimaryDark}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Highlight" Color="{StaticResource PrimaryLight}"/>

            <!-- Navigation Button Style -->
            <Style x:Key="NavigationButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MahApps.Styles.Button.Flat}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Height" Value="45"/>
                <Setter Property="Margin" Value="20,0"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource SurfaceBrush}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource SurfaceBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- MetroWindow Default Style -->
            <Style TargetType="{x:Type mah:MetroWindow}"
                    BasedOn="{StaticResource {x:Type mah:MetroWindow}}">
                <Setter Property="GlowBrush"
                        Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="WindowTransitionsEnabled"
                        Value="False"/>
                <Setter Property="WindowTitleBrush"
                        Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="NonActiveGlowBrush"
                        Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="TitleForeground"
                        Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="Background"
                        Value="{StaticResource BackgroundBrush}"/>
                <Setter Property="BorderThickness"
                        Value="1"/>
                <Setter Property="TitleCharacterCasing"
                        Value="Normal"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
