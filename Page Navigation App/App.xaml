<?xml version="1.0" encoding="UTF-8"?>
<Application x:Class="Page_Navigation_App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Page_Navigation_App"
             xmlns:view="clr-namespace:Page_Navigation_App.View"
             xmlns:vm="clr-namespace:Page_Navigation_App.ViewModel"
             xmlns:utilities="clr-namespace:Page_Navigation_App.Utilities"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Application.Resources>
        <ResourceDictionary>
            <!-- MahApps.Metro ResourceDictionaries -->
            <ResourceDictionary.MergedDictionaries>
                <!-- MahApps Base Styles -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                
                <!-- MahApps Control Styles -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.Buttons.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.DataGrid.xaml" />
                
                <!-- MahApps Theme (Light or Dark) -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- View-ViewModel Mappings -->
            <DataTemplate DataType="{x:Type vm:HomeVM}">
                <view:Home/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:CustomerVM}">
                <view:Customers/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ProductVM}">
                <view:Products/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:OrderVM}">
                <view:Orders/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TransactionVM}">
                <view:Transactions/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SupplierVM}">
                <view:Suppliers/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:RateMasterVM}">
                <view:RateMaster/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:RepairJobVM}">
                <view:RepairJobs/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:StockVM}">
                <view:Stock/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:CategoryVM}">
                <view:Categories/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:UserVM}">
                <view:Users/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReportVM}">
                <view:Reports/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsVM}">
                <view:Settings/>
            </DataTemplate>

            <!-- Converters -->
            <utilities:ViewModelToTitleConverter x:Key="ViewModelToTitleConverter"/>
            <utilities:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <utilities:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <utilities:LoginButtonTextConverter x:Key="LoginButtonTextConverter"/>

            <!-- MahApps Accent Colors -->
            <Color x:Key="MahApps.Colors.AccentBase">#2D98DA</Color>
            <Color x:Key="MahApps.Colors.Accent">#2D98DA</Color>
            <Color x:Key="MahApps.Colors.Accent2">#74B9FF</Color>
            <Color x:Key="MahApps.Colors.Accent3">#2475A2</Color>
            <Color x:Key="MahApps.Colors.Accent4">#45AAF2</Color>
            <Color x:Key="MahApps.Colors.Highlight">#74B9FF</Color>
            <Color x:Key="MahApps.Colors.IdealForeground">White</Color>

            <!-- MahApps Brushes -->
            <SolidColorBrush x:Key="MahApps.Brushes.AccentBase" Color="{StaticResource MahApps.Colors.AccentBase}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Accent" Color="{StaticResource MahApps.Colors.Accent}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Accent2" Color="{StaticResource MahApps.Colors.Accent2}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Accent3" Color="{StaticResource MahApps.Colors.Accent3}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Accent4" Color="{StaticResource MahApps.Colors.Accent4}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Highlight" Color="{StaticResource MahApps.Colors.Highlight}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.IdealForeground" Color="{StaticResource MahApps.Colors.IdealForeground}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.ThemeBackground" Color="{StaticResource MahApps.Colors.Accent4}"/>
            <SolidColorBrush x:Key="MahApps.Brushes.Window.Background" Color="{StaticResource MahApps.Colors.Accent4}"/>

            <!-- Application Brushes (utilizing MahApps colors) -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource MahApps.Colors.Accent}"/>
            <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource MahApps.Colors.Accent2}"/>
            <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource MahApps.Colors.Accent3}"/>
            <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource MahApps.Colors.Accent4}"/>
            <SolidColorBrush x:Key="SurfaceBrush" Color="White"/>
            <SolidColorBrush x:Key="SurfaceSecondaryBrush" Color="#F1F2F6"/>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="White"/>
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="#DFF1FF"/>
            <SolidColorBrush x:Key="TextOnSurfaceBrush" Color="#2D3436"/>
            <SolidColorBrush x:Key="SuccessBrush" Color="#26DE81"/>
            <SolidColorBrush x:Key="WarningBrush" Color="#F1C40F"/>
            <SolidColorBrush x:Key="ErrorBrush" Color="#EB3B5A"/>
            <SolidColorBrush x:Key="InfoBrush" Color="#00CEC9"/>

            <!-- Navigation Button Style -->
            <Style x:Key="NavigationButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MahApps.Styles.Button.Flat}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Height" Value="45"/>
                <Setter Property="Margin" Value="20,0"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource SurfaceBrush}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource SurfaceBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- MetroWindow Default Style -->
            <Style TargetType="{x:Type mah:MetroWindow}"
                   BasedOn="{StaticResource {x:Type mah:MetroWindow}}">
                <Setter Property="GlowBrush" Value="{StaticResource MahApps.Brushes.Accent}"/>
                <Setter Property="WindowTransitionsEnabled" Value="False"/>
                <Setter Property="WindowTitleBrush" Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="NonActiveGlowBrush" Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="TitleForeground" Value="{StaticResource MahApps.Brushes.IdealForeground}"/>
                <Setter Property="Background" Value="{StaticResource MahApps.Brushes.ThemeBackground}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="TitleCharacterCasing" Value="Normal"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
